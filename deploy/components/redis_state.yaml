apiVersion: dapr.io/v1alpha1
kind: Component
metadata:
  name: redis
spec:
  type: state.redis
  version: v1
  metadata:
    - name: redisHost
      value: redis_local:6379  # Redis主机和端口号
    - name: redisPassword
      value: ""  # Redis密码（可选）
